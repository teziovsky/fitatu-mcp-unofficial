services:
  fitatu-mcp:
    build: .
    container_name: fitatu-mcp
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: "8000"
